#include <stdio.h>

main() {

  int first_number;
  int second_number;
  int result;
  
  first_number = 3;
  second_number = 5;
  
  printf("first part: %d", first_number);
  //result = (first_number + second_number) / (second_number - second_number);
  
  printf ("The result is: %d \n", result);
  
}