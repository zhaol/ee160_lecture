#include <stdio.h>

main () {
  float a;
  float b;
  float c;
  float result;
  
  float intermediate1;
  
  printf("enter in numbers\n");
  scanf("%f %f %f", &a, &b, &c);
  intermediate1 = (-b + b*b-4*a*c);
  intermediate2 = (2*a)
  result = intermediate1/intermediate2;
  printf("%f\n", result);
}