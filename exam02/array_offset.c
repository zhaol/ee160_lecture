counter = 0;
counter = 1;

while (counter <= something)
while (counter < something)
while (counter <= something - 1)
while (counter < something - 1)


{
    array[counter];    
    array[counter - 1];    
}

// 1, 2, 3, 4...
// array[0], array[1], array[2]


#define ARRAY_OFFSET -1
#define NEXT 1
counter = 1;

while (counter <= MAX) {
    
    array[counter + NEXT + ARRAY_OFFSET]
    
}