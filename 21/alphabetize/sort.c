#include <stdio.h>

main() {
    char a = 'a';
    char b = 'b';
    
    // loop 1
      // loop 2
        //if(a[index] < b[index]) {
        if(a < b) {
            printf("a\n");
        } else {
            printf("b\n");
        }
      // end
    // end
}